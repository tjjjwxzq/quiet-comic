# Welcome to Jekyll!
#
# This config file is meant for settings that affect your entire site, values
# which you are expected to set up once and rarely need to edit after that.
# For technical reasons, this file is *NOT* reloaded automatically when you use
# `jekyll serve`. If you change this file, please restart the server process.

# Site Settings
locale:            "en"
title:             "My quiet webcomic"
name:              "Your Name"
description:       "A quiet webcomic"
url:               # the base hostname and protocol (eg. http://example.com)
baseurl:           # the subpath of your site eg. /mycomic
repository:        # Github username/repo-name
brand_image:       "assets/images/placeholder-brand-image.png" # path to brand image in the `assets/images` folder
base_image_path:         "assets/images/" # default image directory. Ensure the trailing slash is there
thumbnail_label: "page number" # either "title" or "page_number"

comments:
  facebook:
    # https://developers.facebook.com/docs/plugins/comments
    num_posts:     # 5 (default)
    colorscheme:   # "light" (default), "dark"

# Social sharing
twitter:
  username:
facebook:
  username:
  app_id:
  publisher:
og_image:          # Open Graph site image
# For specifying social profiles
# - https://developers.google.com/structured-data/customize/social-profiles
social:
  type             : # Person or Organization (defaults to Person)
  name             : # If the user or organization name differs from the site's name
links:             # An array of links to social media profiles

# Analytics
google_analytics: # your google analytics tracking code
    # https://analytics.google.com/analytics/web/provision/?authuser=0#provision/SignUp/

# Site author
author:
  name:            "Your Name"
  bio:
  email:
  facebook:
  github:
  instagram:
  twitter:
  youtube:

# Collections
collections:
  pages:
    output:        true
    permalink:     /:collection/:path/ # trailing slash is necessary for current version of jekyll to serve pages correctly in development
    layout:        default
  artworks:
    output:        true
    permalink:     /:collection/:path/
    layout:        default

# Markdown Processing
kramdown:
  input:           GFM

# Sass
sass:
  sass_dir:        _sass
  style:           compressed


# Plugins
plugins:
  -                jekyll-paginate-v2
  -                jekyll-sitemap
  -                jekyll-feed

# Defaults
defaults:
  # comic pages
  -
    scope:
      path: "pages"
      type: "pages"
    values:
      layout: "pages"
  -
    scope:
      path: "artworks"
      type: "artworks"
    values:
      layout: "artworks"
